We have the create game page and it is currently setup for killer.
We are going to need different input options depending on which game is selected.
I will define this below.
If a game needs more than one player (2+ players) is added after the game title

Consistent across all games we need:
- the list of players added
- the number of sets to play to
- the number of legs to win a set
- the dropdown which contains the list of different games

Then we will have different inputs needed for each game.
I will also list killer which is the current implementation which I do not want changing for when killer is selected as an example for my style of defining inputs.

I will list the game type (which should be added to the dropdown and the input options below should change to what is needed for that game) and the inputs that will be needed.


Killer (2+ players):
- What number each player has
- What the target killer score is
- If bullseye buyback is selected
- the number of rounds bullseye buyback is active when bullseye buyback is enabled


Every Number:
- How many hits per number
- Include bullseye


Score Killer (2+ players):
- How many lives per player


X01:
- what number x represents e.g. x=5 means playing 501
- if double in
- if double out


Doubles:
- none needed


Splitscore:
- none needed 


Cricket (2+ players):
- how many numbers to play with
- which numbers to play with
- how many times to hit to open a number


Shanghai:
- how many numbers to play
- scoring based on number value or how many hits


Around The World:
- bullseye in order or 3 points on it
- if you go again with 3 hits on target in one visit
- if you go back if you miss all darts in one visit
- if you go back if you miss a single dart
- does treble 20 win the game