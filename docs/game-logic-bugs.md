X01:
- if we enter how many legs we want to play to in a set AND how many sets to play to then show legs won and sets won and display how many of each in the header.
- when a player wins that many sets then the game ends.
- for example: if someone enters 3 legs to win a set and 3 sets to win the game, then when a player hits 3 legs, they win the set. Then when a player has won 3 sets the game ends.
- when a new leg starts, the player that went second in the previous leg goes first.
- when a new set starts, the player that went second in the first game of the previous set goes first.
- the round does not matter in the header section for this game
- instead of the title being X01, it should be whatever number they start at from the input on the page before (for the quick link on the home page let this be 501 and play first to 3 legs in a set, first to 3 sets)

Every Number:
- For this there is the normal player section we have and then we have the Number Progress section. Player section to not have what is there, I want it to be use the player number section there instead. So the player section contains each player, that just has their name and their number progress and that is it
- If they hit a number more than the required times then their score goes down like it does in the game killer. For example if they need to hit each number 3 times, then someone hits T1 and then 1 then they go down to 2/3 instead. So 4/3 = 2/3, 5/3 = 1/3, 6/3 = 0/3


Splitscore:
- the logic for the game is correct.
- I want the header to say what the player should be aiming for in that round.


Around the world:
- I do not know much about the logic of this game because I think the game is starting at 0, however it needs to start at 1.


Shanghai:
- each round you are aiming for a specific number.
- so the first round you are only aiming for the 1 segment, the second the 2 segmaent and so on, with the highest round possible 20.
- In the header it should say what segment you are aiming for.

Doubles:
- each round you aim for a specific double
- so the first round you are only aiming for the 1 segment, the second the 2 segment and so on, with round 21 being bullseye.
- In the header it should say what segment you are aiming for.


Cricket:
- This game needs work.
- For an example we will start with a classic game of cricket and get that working, then look at editing how many numbers to play with or what numbers to play with.
- So the numbers in play are 15, 16, 17, 18, 19, 20 and Bull
- Once a player gets 3 hits on a number then the number is 'open' for that player.
- When a player hits an 'open' number then they gain points which are the numerical value of what they hit.
- When every player has 'opened' a number, then the number becomes 'closed' for every player and nobody can score points on it.
- The game continues until a player has the most points and all of the numbers have been hit the required number of times 
- For the player boxes, we should show the name of the player, then the numbers that are in play, how many times they have hit it, and how many points the have scored for each number.
- In the header it should have the numbers to hit for the game, with a clear indication if they have been closed for everyone or not.
