Shared:
- player positioning - should not be covered by the header or the scoring component when the scoring component is closed
- scoreboard numbers - on a mobile device all numbers should be visible when the scoring component is opened. If the buttons need to be small or reordered to for them to fit. Currently on my phone I can not click on any number higher than 10
- other than when no darts have been thrown, back should always be enabled in case the player inputs incorrectly
- when showing what players just hit, when a player has a new turn, it should clear. So there will always be 0-3 numbers displayed for what a player has hit.
- players only need unique number for killer, for every other game they do not need this when creating a ga,e
- update what the player hits with every dart, not just after the visit so that as they enter what they hit, they can make sure they inputted correctly

Every Number:
- dont need original player list, remove it, because the number progress is good and this can be used as the player list. So highlight that for a players turn 
- need numbers to go down when too high e.g 4/3 should go to 2/3 or 6/3 goes to 0/3.
- the number of hits on a number should not go beneath 0.


Score Killer:
- overall score does not matter in summation, all that matters is total score of previous visit


X01:
- choose how many legs/sets to play to in create game screen.
- when a leg ends, whichever player went second in the previous game goes first next.
- when a set ends, whichever player went second in the first game of the previous set goes first next.


Doubles:
- Instead of round in the header, say number to aim for. It starts at 1 and increases by 1 after every visit. When it is round 21, it should say bullseye. After the bullseye round the game ends.


Splitscore:
- need to hit specific number each round which should be displayed in the header instead of the round number
- it should have number instead of round and say what the 


Cricket:
- on the create screen, after entering how many numbers they want to play, they need to select which numbers. 
- need a numbers hit shown just like in the game every number


Shanghai:
- the number of rounds can be anything from 1 to 21
- should say what number to aim for instead of round
